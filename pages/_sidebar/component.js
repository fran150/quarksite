define(['quark', 'knockout', 'text!./component.html'], function($$, ko, template) {
    function ComponentSidebar(params, $scope, $imports) {
        var self = this;
    }

    return $$.component(ComponentSidebar, template);
});
