define(['quark', 'knockout', 'text!./redirectHash.html'], function($$, ko, template) {
    function DocsRefsRedirectHashPage(params, $scope, $imports) {
        var self = this;

    }

    return $$.component(DocsRefsRedirectHashPage, template);
});
