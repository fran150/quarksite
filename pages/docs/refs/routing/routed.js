define(['quark', 'knockout', 'text!./routed.html'], function($$, ko, template) {
    function DocsRefsRoutingRoutedPage(params, $scope, $imports) {
        var self = this;

    }

    return $$.component(DocsRefsRoutingRoutedPage, template);
});
