define(['quark', 'knockout', 'text!./behaviours.html'], function($$, ko, template) {
    function DocsRefsBehavioursPage(params, $scope, $imports) {
        var self = this;

    }

    return $$.component(DocsRefsBehavioursPage, template);
});
