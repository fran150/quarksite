define(['quark', 'knockout', 'text!./apply.html'], function($$, ko, template) {
    function DocsRefsBehavioursApplyPage(params, $scope, $imports) {
        var self = this;

    }

    return $$.component(DocsRefsBehavioursApplyPage, template);
});
