define(['quark', 'knockout', 'text!./waitready.html'], function($$, ko, template) {
    function DocsRefsWaitReadyPage(params, $scope, $imports) {
        var self = this;

    }

    return $$.component(DocsRefsWaitReadyPage, template);
});
