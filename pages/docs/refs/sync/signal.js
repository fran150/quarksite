define(['quark', 'knockout', 'text!./signal.html'], function($$, ko, template) {
    function DocsRefsSignalPage(params, $scope, $imports) {
        var self = this;
    }

    return $$.component(DocsRefsSignalPage, template);
});
