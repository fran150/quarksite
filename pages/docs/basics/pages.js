define(['quark', 'knockout', 'text!./pages.html'], function($$, ko, template) {
    function DocsBasicsPagesPage(params, $scope, $imports) {
        var self = this;

    }

    return $$.component(DocsBasicsPagesPage, template);
});
