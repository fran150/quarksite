define(['quark', 'knockout', 'text!./docs.html'], function($$, ko, template) {
    function DocsSidebar(params, $scope, $imports) {
        var self = this;
    }

    return $$.component(DocsSidebar, template);
});
