define(['quark', 'knockout', 'text!./left.html'], function($$, ko, template) {
    function MainLeftNavbar(params, $scope, $imports) {
        var self = this;
    }

    return $$.component(MainLeftNavbar, template);
});
