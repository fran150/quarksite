define(['quark', 'knockout', 'text!./home.html'], function($$, ko, template) {
    function DocsHomePage(params, $scope, $imports) {
        var self = this;
    }

    return $$.component(DocsHomePage, template);
});
