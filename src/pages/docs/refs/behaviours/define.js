define(['quark', 'knockout', 'text!./define.html'], function($$, ko, template) {
    function DocsRefsBehavioursDefinePage(params, $scope, $imports) {
        var self = this;

    }

    return $$.component(DocsRefsBehavioursDefinePage, template);
});
