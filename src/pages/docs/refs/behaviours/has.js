define(['quark', 'knockout', 'text!./has.html'], function($$, ko, template) {
    function DocsRefsBehavioursHasPage(params, $scope, $imports) {
        var self = this;

    }

    return $$.component(DocsRefsBehavioursHasPage, template);
});
