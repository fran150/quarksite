define(['quark', 'knockout', 'text!./signal.html'], function($$, ko, template) {
    function DocsRefsSyncSignalPage(params, $scope, $imports) {
        var self = this;
    }

    return $$.component(DocsRefsSyncSignalPage, template);
});
