define(['quark', 'knockout', 'text!./islocked.html'], function($$, ko, template) {
    function DocsRefsSyncLockIsLockedPage(params, $scope, $imports) {
        var self = this;

    }

    return $$.component(DocsRefsSyncLockIsLockedPage, template);
});
