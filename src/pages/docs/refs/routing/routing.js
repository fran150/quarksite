define(['quark', 'knockout', 'text!./routing.html'], function($$, ko, template) {
    function DocsRefsRoutingPage(params, $scope, $imports) {
        var self = this;

    }

    return $$.component(DocsRefsRoutingPage, template);
});
