define(['quark', 'knockout', 'text!./behaviours.html'], function($$, ko, template) {
    function DocsBasicsBehavioursPage(params, $scope, $imports) {
        var self = this;

    }

    return $$.component(DocsBasicsBehavioursPage, template);
});
