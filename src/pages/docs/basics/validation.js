define(['quark', 'knockout', 'text!./validation.html'], function($$, ko, template) {
    function DocsBasicsValidationPage(params, $scope, $imports) {
        var self = this;
    }

    return $$.component(DocsBasicsValidationPage, template);
});
